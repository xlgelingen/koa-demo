const Base = require("./base");

class QuestionScoreDimensions extends Base {
  constructor(props = "question_score_dimensions") {
    super(props);
  }
}

module.exports = new QuestionScoreDimensions();
